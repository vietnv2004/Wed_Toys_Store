@model Wed_Toys_Store.Models.CategoriesViewModel

@{
    ViewData["Title"] = "Categories Management";
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Admin Panel</title>

    <!-- Bootstrap -->
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" />

    <!-- CSS BẮT BUỘC (Bootstrap không thay thế được) -->
    <style>
        :root {
            --admin-sidebar-bg: #4169E1;
            --admin-sidebar-header: #2E4FC0;
        }

        body {
            background-color: #f5f5f5;
        }

        .admin-sidebar {
            width: 250px;
            height: 100vh;
            background-color: var(--admin-sidebar-bg);
        }

        .admin-main {
            margin-left: 250px;
            min-height: 100vh;
        }
    </style>
</head>
<body>

    <!-- SIDEBAR -->
    @await Html.PartialAsync("_AdminSidebar")

    <!-- MAIN -->
    <div class="admin-main">
        <div class="p-4">

            <!-- MESSAGES -->
            @if (TempData["SuccessMessage"] != null)
            {
                <div class="alert alert-success alert-dismissible fade show" role="alert">
                    <i class="bi bi-check-circle me-2"></i>@TempData["SuccessMessage"]
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            }

            @if (TempData["ErrorMessage"] != null)
            {
                <div class="alert alert-danger alert-dismissible fade show" role="alert">
                    <i class="bi bi-exclamation-triangle me-2"></i>@TempData["ErrorMessage"]
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            }

            <!-- HEADER -->
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h2 class="mb-0 fw-bold">
                    <i class="bi bi-folder me-2"></i>Categories Management
                </h2>
                <a asp-action="Create"
                   asp-controller="Categories"
                   class="btn btn-primary">
                    <i class="bi bi-plus-circle me-2"></i>Create New Category
                </a>
            </div>

            <!-- Search and Filter Section -->
            <div class="card shadow-sm mb-4">
                <div class="card-body">
                    <form method="get" action="@Url.Action("Index", "Categories")" id="searchForm">
                        <input type="hidden" name="page" value="1" id="pageInput">
                        <input type="hidden" name="pageSize" value="@Model.PageSize" id="pageSizeInput">
                        <div class="row g-3 align-items-end">
                            <div class="col-md-3">
                                <label class="form-label small text-muted">Filter</label>
                                <div class="input-group">
                                    <span class="input-group-text bg-light border-end-0">
                                        <i class="bi bi-search text-muted"></i>
                                    </span>
                                    <input type="text" 
                                           class="form-control border-start-0" 
                                           name="searchKeyword" 
                                           id="searchKeyword"
                                           placeholder="Search by name or description..." 
                                           value="@Model.SearchKeyword"
                                           style="border-left: 0;">
                                </div>
                            </div>
                            <div class="col-md-2">
                                <label for="pageSizeSelect" class="form-label small text-muted">Items per page</label>
                                <select class="form-select" id="pageSizeSelect" name="pageSize" onchange="changePageSize(this.value)">
                                    <option value="10" selected="@(Model.PageSize == 10)">10</option>
                                    <option value="20" selected="@(Model.PageSize == 20)">20</option>
                                    <option value="50" selected="@(Model.PageSize == 50)">50</option>
                                    <option value="100" selected="@(Model.PageSize == 100)">100</option>
                                </select>
                            </div>
                            <div class="col-md-7">
                                <label class="form-label small text-muted d-block">&nbsp;</label>
                                <div class="d-flex gap-2">
                                    <button class="btn btn-primary" type="submit">
                                        <i class="bi bi-search me-1"></i>Filter
                                    </button>
                                    <a href="@Url.Action("Index", "Categories", new { pageSize = Model.PageSize })" class="btn btn-outline-secondary">
                                        <i class="bi bi-x-circle me-1"></i>Clear
                                    </a>
                                </div>
                            </div>
                        </div>
                    </form>
                </div>
            </div>

            <!-- TABLE -->
            <div class="card shadow-sm">
                <div class="card-body p-0">
                    <table class="table table-hover align-middle mb-0">
                        <thead class="table-light">
                            <tr>
                                <th class="text-center">ID</th>
                                <th class="text-center">Name</th>
                                <th class="text-center">Description</th>
                                <th class="text-center">Products</th>
                                <th class="text-center">Actions</th>
                            </tr>
                        </thead>
                        <tbody>

                            @if (Model != null && Model.Categories != null && Model.Categories.Any())
                            {
                                foreach (var category in Model.Categories)
                                {
                                    <tr>
                                        <td class="fw-semibold text-center">@category.Id</td>

                                        <td class="fw-semibold text-center">
                                            @category.Name
                                        </td>

                                        <td class="text-muted small text-center">
                                            @(string.IsNullOrEmpty(category.Description)
                                                                                ? "-"
                                                                                : category.Description.Length > 50
                                                                                ? category.Description.Substring(0, 50) + "..."
                                                                                : category.Description)
                                </td>

                                        <td class="text-center">
                                            <span class="badge bg-primary">
                                                @(category.Products?.Count ?? 0)
                                            </span>
                                        </td>

                                        <td class="text-center">
                                            <div class="btn-group btn-group-sm">
                                                <a asp-action="Details"
                                                   asp-route-id="@category.Id"
                                                   class="btn btn-outline-primary"
                                                   title="View">
                                                    <i class="bi bi-eye"></i>
                                                </a>
                                                <a asp-action="Edit"
                                                   asp-route-id="@category.Id"
                                                   class="btn btn-outline-warning"
                                                   title="Edit">
                                                    <i class="bi bi-pencil"></i>
                                                </a>
                                                <a asp-action="Delete"
                                                   asp-route-id="@category.Id"
                                                   class="btn btn-outline-danger"
                                                   title="Delete">
                                                    <i class="bi bi-trash"></i>
                                                </a>
                                            </div>
                                        </td>
                                    </tr>
                                                        }
                            }
                            else
                            {
                                <tr>
                                    <td colspan="5" class="text-center py-5 text-muted">
                                        <i class="bi bi-inbox fs-1 d-block mb-3"></i>
                                        No categories found.
                                        <a asp-action="Create"
                                           asp-controller="Categories"
                                           class="text-primary">
                                            Create your first category
                                        </a>
                                    </td>
                                </tr>
                            }

                        </tbody>
                    </table>
                </div>
            </div>

            <!-- Pagination -->
            @if (Model != null && Model.Categories != null && Model.Categories.Any() && Model.TotalPages > 1)
            {
                <nav aria-label="Page navigation" class="mt-4">
                    <ul class="pagination justify-content-center mb-0">
                        <li class="page-item @(Model.HasPreviousPage ? "" : "disabled")">
                            <a class="page-link" 
                               href="@(Model.HasPreviousPage ? Url.Action("Index", "Categories", new { page = Model.CurrentPage - 1, pageSize = Model.PageSize, searchKeyword = Model.SearchKeyword }) : "#")" 
                               aria-label="Previous"
                               @(Model.HasPreviousPage ? "" : "aria-disabled=\"true\" tabindex=\"-1\"")>
                                <span aria-hidden="true">Previous</span>
                            </a>
                        </li>
                        @{
                            int startPage = Math.Max(1, Model.CurrentPage - 2);
                            int endPage = Math.Min(Model.TotalPages, Model.CurrentPage + 2);
                            
                            if (startPage > 1)
                            {
                                <li class="page-item">
                                    <a class="page-link" href="@Url.Action("Index", "Categories", new { page = 1, pageSize = Model.PageSize, searchKeyword = Model.SearchKeyword })" aria-label="Go to page 1">1</a>
                                </li>
                                @if (startPage > 2)
                                {
                                    <li class="page-item disabled">
                                        <span class="page-link" aria-hidden="true">...</span>
                                    </li>
                                }
                            }
                            
                            for (int i = startPage; i <= endPage; i++)
                            {
                                <li class="page-item @(i == Model.CurrentPage ? "active" : "")">
                                    @if (i == Model.CurrentPage)
                                    {
                                        <a class="page-link" 
                                           href="@Url.Action("Index", "Categories", new { page = i, pageSize = Model.PageSize, searchKeyword = Model.SearchKeyword })"
                                           aria-label="Go to page @i"
                                           aria-current="page">@i</a>
                                    }
                                    else
                                    {
                                        <a class="page-link" 
                                           href="@Url.Action("Index", "Categories", new { page = i, pageSize = Model.PageSize, searchKeyword = Model.SearchKeyword })"
                                           aria-label="Go to page @i">@i</a>
                                    }
                                </li>
                            }
                            
                            if (endPage < Model.TotalPages)
                            {
                                @if (endPage < Model.TotalPages - 1)
                                {
                                    <li class="page-item disabled">
                                        <span class="page-link" aria-hidden="true">...</span>
                                    </li>
                                }
                                <li class="page-item">
                                    <a class="page-link" href="@Url.Action("Index", "Categories", new { page = Model.TotalPages, pageSize = Model.PageSize, searchKeyword = Model.SearchKeyword })" aria-label="Go to page @Model.TotalPages">@Model.TotalPages</a>
                                </li>
                            }
                        }
                        <li class="page-item @(Model.HasNextPage ? "" : "disabled")">
                            <a class="page-link" 
                               href="@(Model.HasNextPage ? Url.Action("Index", "Categories", new { page = Model.CurrentPage + 1, pageSize = Model.PageSize, searchKeyword = Model.SearchKeyword }) : "#")" 
                               aria-label="Next"
                               @(Model.HasNextPage ? "" : "aria-disabled=\"true\" tabindex=\"-1\"")>
                                <span aria-hidden="true">Next</span>
                            </a>
                        </li>
                    </ul>
                </nav>
            }
        </div>
    </div>

    <!-- SCRIPTS -->
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        function changePageSize(pageSize) {
            const form = document.getElementById('searchForm');
            document.getElementById('pageSizeInput').value = pageSize;
            document.getElementById('pageInput').value = 1;
            form.submit();
        }
    </script>
</body>
</html>
